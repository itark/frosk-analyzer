# Changelog — Implementation Order

| Step | Status | What was done |
|---|---|---|
| 0 | ✅ | Initial exploration — `sector`, `dividendYield` on `Security`; HedgeIndex cache understood |
| 1 | ✅ | FX indicators: `EURUSDStrategy`, `USDJPYStrategy`, `AUDUSDStrategy` + CSV tickers |
| 2 | ✅ | DXY + European vol + OMX vs STOXX50: `DXYStrategy`, `VSTOXXStrategy`, `OMXvsSTOXX50Strategy`; volatility cluster rule |
| 3 | ✅ | Rates + yield curve: `TreasuryYieldStrategy`, `YieldCurveSpreadStrategy`; HY OAS + TED Spread skipped (no ticker) |
| 4 | ⏭️ | Swedish KPIF skipped (no Yahoo Finance ticker) |
| 5 | ✅ | `SwedishLongTermMomentumStrategy` (231 lines): quarterly rebalance, 6M/12-1 momentum, golden cross, relative strength vs `^OMX`, PEG/Beta gates, wired at all 5 `StrategiesMap` points |
| 6 | ✅ | SLMS sector cap (`applySectorCap`), tiered HedgeIndex gate (`HedgeIndexTieredRule`), tiered portfolio sizing (`computeTieredTopN`), low-vol filter, dividend yield stored |
| 7 | ✅ | Scheduler activated: Tier 1/2/3 `@Scheduled` crons live in `Scheduler.java` |
| 8 | ✅ | `DailyBreakoutStrategy` (95 lines) + `DailyOversoldBounceStrategy` (111 lines); `GET /dagstrategin/watchlist`; called from `syncTier1()` every weekday |
| 9 | ✅ | Portfolio quality gate (`passesQualityGate` in `PortfolioService`): SQN ≥ 1.0, win rate ≥ 35%; HighLander + ShortTermMomentum capped at top-10 by SQN; `ShortTermMomentumLongTermStrengthStrategy` now exits on HedgeIndex risk-off OR 15% stop-loss |
| 10 | ✅ | Validated step 9: build compiles cleanly, all strategies wired at 5 points in `StrategiesMap`, `TestJStrategiesMap` passes, all rule classes (`HedgeIndexTieredRule`, `StopLossRule`, `HedgeIndexRiskOffRule`) verified |
| 11 | ✅ | Added 20% hard stop-loss to `HighLanderStrategy` via `StopLossRule`; extracts composite entry/exit rules and appends `.or(stopLoss)` to exit; `frosk.highlander.stoploss.percent=20.0` in `application.properties` |
| 12 | ✅ | Removed `NasdaqVsSPStrategy` from `HedgeIndexService.update()` and its converter method; removed from `frosk.strategies.hedge.strategy` in all properties files (main, test, prod). Strategy remains in `StrategiesMap`. HedgeIndex now has 13 contributing strategies. |
| 13 | ✅ | Created `BreakoutProfitTargetRule` — 2:1 R:R exit based on entry price vs 5-bar low stop level at entry. Added to `DailyBreakoutStrategy` exit rule: `stopLoss.or(profitTarget).or(hedgeRiskOff)`. |
| 14 | ✅ | Already implemented — `PortfolioService.PORTFOLIO_STRATEGIES` allowlist (3 equity strategies only) already excludes all macro/hedge strategies; H2 query already has `fs.name != 'HedgeIndexStrategy'` filter. No code change needed. |
| 15 | ✅ | Same-day idempotency check in `StrategyExecutor`: `lastRunDate` (`LocalDate`) column on `FeaturedStrategy`; skip guard in `execute()` checks if already run today; `frosk.strategy.force.rerun=false` (main) / `true` (test). |
| 16 | ✅ | Same-day idempotency check in `PortfolioService.build()`: skips rebuild if a snapshot already exists for today's local date; `frosk.portfolio.force.rebuild=false` (main) / `true` (test). `PortfolioRepository.existsBySnapshotDateBetween()` used for the check. |
| 17 | ✅ | **Intraday pipeline (Tier 0)**: `IntradayBar` + `IntradaySignal` entities; `IntradayBarRepository` + `IntradaySignalRepository`; `IntradayDataService` (fetches 5m bars for all OMX30 dataset securities → upsert DB → build BarSeries per security → prune old bars); `OMX30IntradayMomentumStrategy` (EMA5/EMA13/RSI5, MaxBarsHeld 12-bar exit); `RunawayGAPIntradayStrategy` (thin subclass of `RunawayGAPStrategy` with own class name for separate FeaturedStrategy rows); `IntradayStrategyRunner` (orchestrator → runs both strategies per OMX30 security → emits BUY/SELL signals to `intraday_signal` → calls `StrategyExecutor` for FeaturedStrategy backtest). Scheduler Tier-0 cron `0 */10 9-17 * * MON-FRI` wired via `syncTier0()` in `HighLander` (gated by `frosk.run.intraday`). Both strategies wired into `StrategiesMap` (all 5 points) and included in `PortfolioService.PORTFOLIO_STRATEGIES`. Excluded from batch all-strategies run. Intraday strategies use `TradeOnCurrentCloseModel` with 0.03% fee (`intradayFeePerTradePercent`). Fixed `RapidApiManager.Interval` visibility (now `public`) and removed spurious URL-parameter spaces in `getHistory()`. Fixed `RunawayGAPIndicator.getUnstableBars()` infinite recursion. |
| 18 | ✅ | **Nordic Quant Investment Strategy Agent — trader-facing REST + UI layer**: 4 new REST endpoints in `DataController`: `GET /intraday/signals` (latest 20 signals as `IntradaySignalDTO`), `GET /intraday/signals/today` (today's BUY signals only), `GET /hedgeindex/score` (current score + regime label as `HedgeIndexScoreDTO`), `GET /hedgeindex/history?days=30` (daily score series). New DTOs: `IntradaySignalDTO`, `HedgeIndexScoreDTO`. React dashboard at `src/main/resources/static/index.html` (CDN React 18 + Recharts, no build tooling). 4 panels: HedgeIndex gauge with area chart history, intraday signals table (Stockholm time), Dagstrategin watchlist (reuses `/dagstrategin/watchlist`), portfolio snapshot (reuses `/portfolio`). Auto-refreshes every 60s. Dark theme. |
| 19 | ✅ | **Intraday Investor Dashboard — backend endpoints**: `GET /intradayOpenPositions` (securities where `OMX30IntradayMomentumStrategy` has an open position; response: `[{ securityName, entryPrice, entryTime, currentPrice, unrealizedPnl }]`). `GET /intradayTodaySignals` (all buy/sell signals fired today; response: `[{ securityName, type, price, date in "YYYY-MM-DD HH:mm" }]`). New DTOs: `IntradayOpenPositionDTO`, `IntradayTodaySignalDTO`. Also added `POST /intraday/run` for manual trigger outside market hours. |
| 20 | ✅ | **Intraday Investor Dashboard — frontend page**: Three-section page at `/intraday-strategies-page.html`: (1) Active Signals card — fetches `/intradayOpenPositions`, shows live positions with entry time, price, current price, unrealized PnL; (2) Today's Trades card — fetches `/intradayTodaySignals`, actionable feed of today's BUY/SELL signals; (3) Strategy Scorecard — aggregates from `/featuredStrategies` filtered to `OMX30IntradayMomentum` (avg SQN, win rate, expectancy, total trades) + per-security table. CDN React 18 + dark theme, auto-refresh 60s. |

> **Results are queryable directly in the H2 console** (`/h2-console`). New endpoints should only be added when there is a concrete consumer (e.g. a frontend or external integration).

## 2026-06-11 — PnL overhaul (daily + intraday portfolios)

| Step | Status | What was done |
|---|---|---|
| P1 | ✅ | **Validation harness**: `StrategyComparisonReportIT` (`#dailyReport`, `#intradayReport`) runs strategies through the production execution path (same fees/trade models) and prints per-strategy aggregates without DB writes. Run with `mvn test -Dmaven.test.skip=false -Dtest=StrategyComparisonReportIT#dailyReport`; override strategy list via `-Dfrosk.compare.strategies=...`. `IntradayDataService.buildAllSeriesFromDb()` added for offline series building. |
| P2 | ✅ | **HedgeIndex semantics fixed**: rows are state-change events, but the cache counted events on exact-millisecond dates — `getScore(now())` and any intraday timestamp returned 0, so tiered sizing and regime gates were dead code. Cache now replays events chronologically carrying each indicator's state forward, keyed by Stockholm start-of-day in a `TreeMap` with `floorEntry` lookup. `frosk.hedge.criteria.risk.threshold` recalibrated 2→7 (level semantics, 8+ = defensive). New `HedgeIndexMaxScoreRule` works on daily and 15m bars. Test: `TestJHedgeIndexDayScore`. |
| P3 | ✅ | **Reusable rules**: `AtrStopLossRule`, `AtrTrailingStopRule` (chandelier), `MaxBarsHeldRule` extracted to `strategies/rules/`; inner-class duplicates removed from OMXS30Swing + 3 intraday strategies. |
| P4 | ✅ | **ShortTermMomentumLongTermStrength redesign**: entry = fresh `CrossedUp(sma10, sma20)` + close>SMA50/100 + ADX(14)>25 + HedgeIndex≤7; exit = 2.5×ATR trailing stop OR `CrossedDown(close, sma50)` OR score>9 OR 10% stop (SMA10<SMA20 whipsaw exit deleted). Test-DB backtest: 6,802 trades at −0.60%/trade → 158 trades at **+1.48%/trade**, avg maxDD 64%→19%. |
| P5 | ✅ | **HighLander redesign**: macro composite now requires a price trigger (`CrossedUp(ema20, ema50)` + close>SMA200); exit adds 3×ATR trailing stop; hard stop 20%→12%. Backtest: −4.19%/trade → **+13.7%/trade** (8 high-quality trades, maxDD 55%→14%). |
| P6 | ✅ | **Månadsportföljen actually trades**: monthly window (first 7 days) instead of quarterly, 12M−1 momentum rule dropped, volatility cap computed on **returns** (was price levels — blocked every mover), maxVolatility 0.60, own PEG key (2.5), exit on ROC6m<−5 / score>9 / death cross / 3×ATR trail. Backtest: 2 trades ever → **135 trades at +1.23%/trade** (34% win). |
| P7 | ✅ | **Portfolio gates**: win-rate gate scale bug fixed (`frosk.portfolio.min.win.rate=40.0`, percent scale — 0.35 was a no-op against percent-stored values); `DailyOversoldBounceStrategy` + `CANSLIMStrategy` added to `DAILY_STRATEGIES`; `computeTieredTopN()` uses day-floored score; `other.topN` 10→12. Test: `TestJPortfolioQualityGate`. |
| P8 | ✅ | **SessionVWAPIndicator**: true session-anchored VWAP (Σ typical×vol / Σ vol since Stockholm day start). Test: `TestJSessionVWAPIndicator`. |
| P9 | ✅ | **Intraday redesigns**: VWAP-MR uses real session VWAP (entry ≥0.15% below, exit at VWAP, 0.6% stop, 12 bars); ORB entry is a fresh cross (no re-entry chains), morning-only (≤bar 12), ±1.5% gap filter, 16-bar/0.8% stops; GapReversal widened to 0.5–3.5% gaps, ≤bar 12, ATR stop with 0.6% floor. All three gated by `frosk.intraday.hedge.max.score=9` (a gate at 7 stopped all trading in defensive weeks). `intraday.retention.days` 7→30 so future evaluation has a real sample. Net/trade on the 7-day sample: ORB −0.14, VWAP-MR −0.10 (was −0.19), GapReversal **+0.07 at 2.3× trade count**. |
| P10 | ✅ | **Intraday portfolio PnL is now additive day-PnL**: `Portfolio.realizedPnlPercent` + `closedTradeCount`; `buildIntraday()` pairs today's BUY→SELL `IntradaySignal`s, nets 2×0.03% fee, `totalPnlPercent` = realized + open unrealized (the old open-positions-only average read 0.0000 forever). `GET /intraday/pnl` also nets fees now. Test: `TestJIntradayPortfolioPnl`. |
| P11 | ✅ | **Crypto intraday pipeline (Phases 1–2 of the crypto overhaul)**: `CryptoIntradayDataService` fetches 15m candles for `crypto.intraday.products` (BTC-EUR, ETH-EUR, SOL-EUR) via the **public** Coinbase market-data endpoint (`ProductService.getPublicCandles` — no JWT signing, zero RapidAPI cost), chunked at 300 candles/request, skips the still-forming candle, persists into `intraday_bar`, prunes by `intraday.retention.days`. Scheduler `cryptoIntradaySync()` every 15 min around the clock (`scheduler.crypto.intraday.cron`, enabled in the crypto profile only) via `HighLander.syncCryptoIntraday()` gated by `frosk.run.crypto`. **Fee model**: `exchange.transaction.cryptoTakerFeePerTradePercent=0.006` / `cryptoMakerFeePerTradePercent=0.004`; `BarSeriesService.resolveFee()` maps future crypto intraday strategies to the taker fee — the 0.03% equity intraday fee would overstate crypto results ~20×. Also fixed: `application-equity.properties` had `frosk.runallstrategies=false` overriding the base property (equity batch never ran). Verified: `TestJCryptoIntradaySync` (network) pulled 671 live 15m BTC-EUR bars. Crypto-native strategies (rolling-range breakout, UTC-day VWAP reversion, BTC regime gate) are Phase 3. |
| P12 | ✅ | **Crypto intraday Phases 3–5**: (3) `CryptoRangeBreakoutIntradayStrategy` (rolling 6h Donchian breakout, fresh-cross entry, min range width 1.5% so the move can pay its 1.2% taker round-trip, 24h EMA trend filter, 1.5×ATR stop + 2.5×ATR trail, 24h time stop) and `CryptoVWAPReversionIntradayStrategy` (UTC-day `SessionVWAPIndicator` — now zone-parameterized — stretch 1.8% = gross target, RSI<30, stop 2.7%, 12h time stop). Both gated by `CryptoRegimeRule`/`CryptoRegimeService`: risk-on only while BTC-EUR closes above its daily SMA(20), day-floored cache, fails closed. New `CryptoIntradayStrategy` marker interface — deliberately separate from `IntradayStrategy` so crypto strategies never run in the equity Tier-0 runner. Registered at all 5 `StrategiesMap` points, excluded from batch runs, mapped to the 0.6% taker fee in `BarSeriesService.CRYPTO_INTRADAY_STRATEGIES` and to `TradeOnCurrentCloseModel`. (4) `StrategyComparisonReportIT#cryptoIntradayReport` — syncs live Coinbase bars (30-day backfill now supported via backward `fetchRange`), seeds securities, reports net of the 1.2% round-trip. (5) `CryptoIntradayStrategyRunner` — paper-trading loop (signals to `intraday_signal`, FeaturedStrategy backtests, NO orders; `frosk.runbot` stays false); wired into `syncCryptoIntraday()` with `portfolioService.buildIntraday()`; crypto strategy names added to `PortfolioService.INTRADAY_STRATEGIES` (separate DBs make the shared list safe). Baseline on 30 days (BTC below SMA20 in 28/30 days — regime gate correctly near-silent): breakout 3 trades net −1.5%/tr, VWAP-reversion 2 trades net −0.7%/tr — sample too small to tune against; accumulate paper-trade evidence and re-evaluate when the regime turns. Also: stripped CRLF line endings in both property files (a `\r` inside `frosk.strategies.exclude` corrupted a strategy name). |
| P13 | ✅ | **Removed `RapidApiManager`**: the class had already been fully superseded by `YahooFinanceDirectClient` (all of `YAHOODataManager` — daily history + the four metadata modules — call the direct client) and was injected nowhere. Deleted `RapidApiManager.java`, dropped the now-unused `rapid.api.key`/`rapid.api.host` (`application.properties`) and `rapid.api.enabled` (`application-prod.properties`), and refreshed all current-state docs/comments (`CLAUDE.md`, `docs/operations.md`, `docs/intraday-pipeline.md`, `docs/architecture.md`, `docs/data-model.md`, `docs/strategies/{equity-selection,manadsportfoljen,dagstrategin}.md`, `Scheduler.java`, `CryptoIntradayDataService.java`) to name `YahooFinanceDirectClient` as the single Yahoo data layer — no API key, no quota, no per-request cost; the tier split now serves load-spreading/freshness, not a request budget. The DTO model package `nu.itark.frosk.rapidapi.yhfinance.model` is **retained** — `YahooFinanceDirectClient` parses Yahoo's JSON into those classes. |
